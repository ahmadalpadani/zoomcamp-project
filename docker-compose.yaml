version: "3.9"

volumes:
  kestra_postgres_data:
  kestra_data:
  pgadmin_data:

services:

  dbt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dbt-container
    ports:
      - "5000:5000"  # Port untuk dbt
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/keys/gcp-key.json
      - DBT_PROFILES_DIR=/app/dbt/zoomcamp
    volumes:
      - .:/app
    command: ["bash", "-c", "/app/dbt/venv/bin/dbt run"]

  spark:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spark-container
    ports:
      - "4040:4040"  # Spark UI untuk monitoring jobs
      - "7077:7077"  # Spark master port
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/keys/gcp-key.json
    volumes:
      - .:/app
    command: ["bash", "-c", "/app/spark/venv/bin/spark-submit /app/spark/notebook/spark-gcs.py"]

  kestra_postgres:
    image: postgres:16
    container_name: kestra_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: kestra
      POSTGRES_USER: kestra
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - kestra_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d kestra -U kestra"]
      interval: 10s
      timeout: 5s
      retries: 10

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: root
    ports:
      - "8085:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      kestra_postgres:
        condition: service_healthy

  kestra:
    image: kestra/kestra:v1.1
    container_name: kestra
    restart: unless-stopped
    pull_policy: if_not_present
    user: root
    command: server standalone

    env_file:
      - .env

    volumes:
      - kestra_data:/app/storage
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp/kestra-wd:/tmp/kestra-wd
      - ./keys:/app/keys
      - ./:/workspace

    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /app/keys/gcp-key.json

      KESTRA_CONFIGURATION: |
        datasources:
          postgres:
            url: jdbc:postgresql://kestra_postgres:5432/kestra
            driverClassName: org.postgresql.Driver
            username: kestra
            password: ${POSTGRES_PASSWORD}

        kestra:

          ai:
            type: gemini
            gemini:
              model-name: gemini-2.5-flash
              api-key: ${GEMINI_API_KEY}

          server:
            basicAuth:
              username: admin@kestra.io
              password: Admin1234

          repository:
            type: postgres

          storage:
            type: local
            local:
              basePath: "/app/storage"

          queue:
            type: postgres

          tasks:
            tmpDir:
              path: /tmp/kestra-wd/tmp

          url: http://localhost:8080/

    ports:
      - "8080:8080"
      - "8081:8081"

    depends_on:
      kestra_postgres:
        condition: service_healthy